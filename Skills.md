# Skills.md - 투자 데이터 자동 대시보드 생성 규칙

## 1. 목적

이 Skill은 주가, ETF, 포트폴리오, 시장 지표, 거래 데이터처럼 구조가 서로 다른 투자 데이터를 입력받아 동일한 분석 절차로 대시보드를 생성하기 위한 범용 지침이다. 목표는 사람이 차트를 직접 고르는 방식이 아니라, 데이터 스키마와 품질을 먼저 판단한 뒤 지표, 시각화, 인사이트를 자동 선택하는 것이다.

## 2. 입력 데이터 범위

허용 데이터는 CSV, 스프레드시트, JSON 테이블, API 응답 테이블이다. 최소 권장 컬럼은 `date`, `ticker`, `close`이며, 있으면 활용할 컬럼은 `return`, `weight`, `value`, `volume`, `benchmark`, `sector`, `currency`이다.

컬럼명이 다르더라도 다음 의미로 매핑한다.

| 의미 | 우선 탐지명 | 처리 기준 |
| --- | --- | --- |
| 시간 축 | date, time, 일자, 날짜, 기준일, 거래일 | 날짜형으로 변환하고 오름차순 정렬 |
| 자산 식별자 | ticker, symbol, asset, name, 종목, 종목명, 자산 | 없으면 단일 자산 데이터로 처리 |
| 가격 | close, price, nav, 종가, 가격, 기준가, 현재가 | 수익률이 없을 때 변화율 계산에 사용 |
| 수익률 | return, ret, 수익률, 등락률, 변화율 | 백분율 또는 소수 형식을 소수 수익률로 표준화 |
| 편입 비중 | weight, allocation, 비중, 편입, 배분 | 합계가 1이 되도록 정규화 |
| 평가 금액 | value, amount, market_value, 평가금액, 잔고, 평가액 | 비중 부재 시 평가금액 비중으로 변환 |
| 거래량/거래대금 | volume, turnover, 거래량, 거래대금, 유동성 | 유동성 보조 지표와 품질 점검에 사용 |

## 3. 분석 절차

1. 데이터 행 수, 컬럼 수, 날짜 파싱 가능 여부를 검사한다.
2. 시간 축과 자산 식별자를 감지한다.
3. 수익률 컬럼이 있으면 그대로 사용하고, 없으면 자산별 가격 변화율로 계산한다.
4. 비중 컬럼이 있으면 최신 비중을 정규화하고, 없으면 평가 금액 비중을 사용한다.
5. 비중과 평가 금액이 모두 없으면 동일 비중 포트폴리오로 가정하되 대시보드에 대체 규칙을 표시한다.
6. 자산별 수익률, 변동성, 누적 성과, 최대 낙폭, 상관계수를 계산한다.
7. 데이터 품질 경고와 분석 신뢰도를 함께 표시한다.

## 4. 대체 계산 규칙

| 상황 | 적용 규칙 | 화면 표시 |
| --- | --- | --- |
| 수익률 컬럼 없음 | 자산별 가격 변화율로 수익률 계산 | 가격 기반 수익률 배지 |
| 비중 컬럼 없음 | 평가금액 기준으로 비중 계산 | 평가금액 비중 배지 |
| 비중과 평가금액 모두 없음 | 동일 비중 포트폴리오로 가정 | 동일 비중 가정 경고 |
| 단일 자산 데이터 | 상관 분석은 제한하고 개별 리스크 분석 중심으로 구성 | 상관 히트맵 제한 사유 |
| 관측일 부족 | 품질 점검 영역에 경고 표시 | 관측치 부족 신뢰도 경고 |

## 5. 필수 지표 계산 규칙

### 일간 수익률

수익률 컬럼이 없으면 가격 변화율로 계산한다.

`return_t = price_t / price_(t-1) - 1`

### 누적 수익률

일별 포트폴리오 수익률 `r_t`를 복리로 누적한다.

`equity_t = equity_(t-1) * (1 + r_t)`

### 연율 수익률

`annual_return = (1 + total_return) ^ (252 / n) - 1`

### 연율 변동성

`annual_volatility = stdev(daily_returns) * sqrt(252)`

### Sharpe Ratio

무위험 수익률이 없으면 0으로 두고 계산한다.

`sharpe = annual_return / annual_volatility`

### 최대 낙폭

`drawdown_t = equity_t / max(equity_0...equity_t) - 1`

최대 낙폭은 전체 기간의 최소 drawdown이다.

### 상관계수

자산이 2개 이상이고 자산별 수익률 배열이 존재할 때 피어슨 상관계수를 계산한다.

## 6. 시각화 선택 규칙

| 조건 | 차트 | 목적 |
| --- | --- | --- |
| 날짜와 수익률 또는 가격이 있음 | 누적 성과 라인 차트 | 기간별 성과 추세 확인 |
| 누적 성과 계산 가능 | 낙폭 분석 차트 | 고점 대비 손실 구간 확인 |
| 비중 또는 평가금액 있음 | 자산 배분 차트 | 포트폴리오 구성 확인 |
| 자산이 2개 이상 | 수익-위험 맵 | 자산별 성과와 변동성 비교 |
| 자산별 수익률 배열 존재 | 상관계수 히트맵 | 분산 투자 효과 확인 |
| 거래량 컬럼 존재 | 유동성 보조 지표 | 데이터 해석 보조 |

차트가 선택되지 못한 경우 빈 영역을 두지 말고, 필요한 컬럼과 대체 분석 방법을 안내한다.

## 7. 인사이트 생성 규칙

인사이트는 항상 숫자 근거를 포함한다. 최소 5개를 생성한다.

1. 성과 판정: 누적 수익률, 연율 변동성, Sharpe를 함께 해석한다.
2. 손실 방어: 최대 낙폭이 -20%보다 낮으면 리스크 경고로 표시한다.
3. 성과 기여 자산: 기간 수익률이 가장 높은 자산을 표시한다.
4. 변동성 원천: 연율 변동성이 가장 높은 자산을 표시한다.
5. 분산도: 최대 편입 비중과 평균 상관계수를 함께 해석한다.

문장은 투자 권유가 아니라 분석 보조 의견으로 작성한다.

## 8. 데이터 품질 규칙

다음 조건은 경고로 표시한다.

- 날짜 컬럼을 감지하지 못한 경우
- 자산 식별자 컬럼을 감지하지 못한 경우
- 가격과 수익률을 모두 감지하지 못한 경우
- 관측일이 30일 미만인 경우
- 원본 행 중 분석에서 제외된 행이 있는 경우
- 단일 자산만 존재하여 상관 분석이 제한되는 경우
- 대체 계산 규칙이 적용된 경우

## 9. 대시보드 UI 구성 규칙

첫 화면은 실제 분석 화면이어야 하며 별도 랜딩 페이지를 두지 않는다. 좌측에는 데이터 선택, CSV 업로드, Skill 실행 단계, 감지된 스키마, 대체 계산 배지를 배치한다. 본문 상단에는 핵심 KPI를 배치하고, 그 아래에 성과, 낙폭, 배분, 수익-위험, 상관 구조를 배치한다. 하단에는 자동 인사이트, 데이터 품질 점검, 분석에 사용된 관측치를 표시한다.

## 10. 확장 규칙

향후 기능을 추가할 때는 이 문서에 먼저 규칙을 추가한 뒤 구현한다. 예를 들어 환율 변환, 벤치마크 초과 수익률, 정보비율, 팩터 노출, 리밸런싱 시뮬레이션, 세금 반영 수익률은 각각 별도 Skill 섹션으로 분리한다.

## 11. 투자 정보 제공 범위

본 서비스는 투자 판단을 직접 권유하지 않으며, 업로드된 데이터에 대한 통계적 분석과 시각화를 제공하는 의사결정 보조 도구이다. 생성된 인사이트는 투자 조언이 아니라 데이터 기반 요약 정보로 활용한다.
